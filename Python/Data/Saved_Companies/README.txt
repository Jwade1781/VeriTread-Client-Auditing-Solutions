These are a list of companies that have been inputted into the system. 
Each companies attributes are entered into a .csv file.
These could be alternatively be stored into a SQL database later and pulled individually based on their DoT numbers.
Created initially here for convenience, i.e. not having to setup local server.

Used in Programs:
Determine_Elig.py -> Pulled to input each companies attributes into the trained ML model